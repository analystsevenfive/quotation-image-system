# Set these variables in the PowerShell terminal that launches the backend.
# The API does not automatically load this file.
# Leave DATABASE_URL unset to use the local JSON snapshot during development.
PRODUCTS_JSON=.data/products.json
# DATABASE_URL=postgresql://backend_user:password@host:5432/postgres?sslmode=require
